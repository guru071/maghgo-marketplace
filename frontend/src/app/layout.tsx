import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Maghgo — Your Chats. Your Web Store.",
  description:
    "Turn WhatsApp, Instagram or Messenger into a premium web store. Send a photo of your product and we do the rest — or manage everything from the web.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
